# requirements_v4.txt — Kids Shorts Bot v6
# Zero cost. No credit card. No billing.

# ── CORE AI & API ────────────────────────────────────────
groq>=0.8.0                    # LLM for scene prompts — free tier
gradio_client>=0.16.0          # HF Space Image-to-Video — free public spaces
pytrends>=4.9.2                # Google Trends topic picker — free

# ── HTTP & MEDIA ─────────────────────────────────────────
requests>=2.31.0               # Pollinations.ai image download — free
Pillow>=10.0.0                 # Image handling — free

# ── YOUTUBE UPLOAD ───────────────────────────────────────
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.1.0

# ── UTILITIES ────────────────────────────────────────────
pytz>=2024.1

# ── SYSTEM (install separately, NOT via pip) ─────────────
# FFmpeg: sudo apt install ffmpeg
# GitHub Actions: auto-installed in workflow

# ══════════════════════════════════════════════════════════
# COST BREAKDOWN
# ══════════════════════════════════════════════════════════
# Tool                    Cost    Card?
# ─────────────────────   ──────  ──────
# Groq API                $0.00   NO
# HuggingFace Spaces      $0.00   NO
# Pollinations.ai         $0.00   NO
# Mixkit music CDN        $0.00   NO
# FFmpeg                  $0.00   NO
# YouTube Data API v3     $0.00   NO
# GitHub Actions (public) $0.00   NO
# ─────────────────────   ──────  ──────
# TOTAL                   $0.00   NEVER
# ══════════════════════════════════════════════════════════

# ── GITHUB SECRETS NEEDED ────────────────────────────────
# GROQ_API_KEY       → console.groq.com (free)
# YT_REFRESH_TOKEN   → from OAuth flow
# YT_CLIENT_ID       → console.cloud.google.com
# YT_CLIENT_SECRET   → console.cloud.google.com
